#ifndef GUARD_find_if_h
#define GUARD_find_if_h

#include "find_if.hpp"

template <class forwardIterator, class predicate>
forwardIterator find_if(forwardIterator, forwardIterator, predicate);

#endif // GUARD_find_if_h
